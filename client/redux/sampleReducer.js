import redux from "redux";

// Action Types


// Action Creators


// Thunks


// Initial State
const INITIAL_STATE = {};

function reducer (state = INITIAL_STATE, action) {
  switch (action) {
    default:
      return state
  }
}

export default reducer;
